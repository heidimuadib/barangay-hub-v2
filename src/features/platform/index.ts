/** Platform feature — Slice 1 exposes read-only console queries. */
export {
  listPlatformAssignments,
  listTenants,
  type PlatformAssignmentRow,
  type TenantRow,
} from './services/platform-console-service'
