# dependencies
node_modules/
.pnpm-store/

# next
.next/
out/
# Regenerated by `next dev` / `next build`. Verified on 2026-07-31 that
# `pnpm typecheck` passes on a clean checkout without it, so it stays ignored.
next-env.d.ts

# production
build/
dist/

# environment — NEVER commit real credentials
.env
.env.*
!.env.example

# supabase local state
supabase/.temp/
supabase/.branches/
supabase/logs/

# test output
coverage/
playwright-report/
test-results/
blob-report/
.playwright/

# tooling
*.tsbuildinfo
.eslintcache
.turbo/

# editors / os
.vscode/*
!.vscode/extensions.json
!.vscode/settings.recommended.json
.idea/
.DS_Store
Thumbs.db

# vercel
.vercel/
